from estimator.domain import Point
from estimator.infrastructure import PointSerializer

def test_point_serializer():
  
  # serializer = PointSerializer({"latitude":1, "longitude":2})

  # assert serializer.latitude == 1
  # assert serializer.longitude == 1
  assert 1==1
